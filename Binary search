class Solution {
    public int mySqrt(int x) {
        int ans=0;
        if(x==0) return 0;
        for(int i=1;i*i<=x;i++){
            if(i*i<=x) ans=i;
            if(i*i>x) break;
        }
        return ans;
    }
}
